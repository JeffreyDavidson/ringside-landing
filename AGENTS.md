# Project instructions

## Git branch naming

- Use `feature/...` for new landing-page features and redesigns.
- Use `feat/...` when the shorter feature prefix better matches the change.
- Do not create branches with a `codex/...` prefix in this repository.
- Open feature pull requests against `main` and use squash merges.
